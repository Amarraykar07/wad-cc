Ubuntu VM password - amar@2026
Kali VM password - kali@2026


1-Nat network setup create and attach both vm to nat network
file-tools-network-nat nw - create
vm(2) machine - setting - nt -net adapter-attached to nat net- created nat net

2)run both vm

ip add by ifconfig 
 check connections of both vm -- ping ip of other vm

3) ssh server status check
sudo apt install openssh-server -y
sudo apt install net-tools -y
sudo systemctl status ssh ( checking active status)
if inactive = sudo systemctl start ssh

sudo systemctl enable ssh 

create file 
nano file1.txt 

scp file1.txt file2.txt nameofvm@<IPof other vm>:/home/kali/ (path to store)